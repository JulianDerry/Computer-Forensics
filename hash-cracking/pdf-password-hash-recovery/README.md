# PDF Password Hash Recovery

| Field | Value |
|---|---|
| **Case Reference** | Microsoft Office 2013 Password Recovery |
| **Image Source** | HIVE CONSULT |
| **Investigator** | Julian Derry |
| **Date of Analysis** | ‎February ‎19, ‎2026 |
| **Platform** | Kali Linux |
| **Tools** | pdf2john.py, Hashcat, pdfinfo |
---
# Table of Contents

1. [Overview](#overview)
2. [Objective](#objective)
3. [Methodological Note](#methodological-note)
4. [Evidence Information](#evidence-information)
5. [Initial Tool Selection](#initial-tool-selection)
6. [PDF Hash Extraction](#pdf-hash-extraction)
7. [Hash Preservation](#hash-preservation)
8. [Hash Mode Identification](#hash-mode-identification)
9. [PDF Version Identification](#pdf-version-identification)
10. [Hashcat Testing](#hashcat-testing)
11. [Password Recovery](#password-recovery)
12. [Verification](#verification)
13. [Tools Used](#tools-used)
14. [Lessons Learned](#lessons-learned)
15. [Conclusion](#conclusion)
16. [Appendix](#appendix)

---

## Overview

This exercise involved recovering and cracking the password hash from a protected PDF file named `logs_protected.pdf`. The file was provided by HIVE CONSULT as a practical password-recovery challenge.

The investigation documented the full workflow from initial tool selection through successful password recovery. During the process, several important lessons were learned regarding file-type identification, correct hash extraction methods, shell handling of special characters, and PDF hash-mode selection in Hashcat.

The objective was to extract the password hash from the protected PDF, determine the appropriate cracking mode, recover the password using a wordlist attack, and verify successful access to the document.

<img width="872" height="498" alt="1" src="https://github.com/user-attachments/assets/c6d0b09c-8f77-4968-b592-a134208286de" />

---

## Methodological Note

This examination was conducted during my early practical training period in digital forensics. At the time of the analysis, a cryptographic integrity hash (for example, SHA-256) of the original pdf was **not calculated prior to examination**.

The omission occurred because I was still developing my forensic workflow and was not yet aware of the importance of documenting evidence integrity before analysis. My current forensic practice includes hashing evidence files before examination and recording the resulting hash values as part of the chain of custody and integrity verification process.

No retrospective hash values have been added to this report.

---

## Objective

Recover the password protecting `logs_protected.pdf` by extracting its hash and performing a controlled password-cracking exercise.

---

## Evidence Information

- **Target File:** `logs_protected.pdf`
- **Source:** @redhadpentester / @hiveconsult
- **Analysis Date:** February 19, 2026

---

## Initial Tool Selection

I first attempted to extract a hash using:

```bash
office2john logs_protected.pdf
```

The command returned:

```text
logs_protected.pdf : Invalid OLE file
```

This demonstrated that **office2john** is intended for Microsoft Office documents such as Word, Excel, and PowerPoint files, not PDF files.

### Screenshot Placeholder

<img width="321" height="82" alt="2" src="https://github.com/user-attachments/assets/44f5f03a-7e93-4271-9f74-0aba935d4ced" />

---

## PDF Hash Extraction

After researching PDF-specific extraction methods, I identified **pdf2john.py** as the correct tool.

Command used:

```bash
pdf2john.py logs_protected.pdf
```

The command successfully extracted the PDF password hash.

### Screenshot Placeholder

<img width="1149" height="95" alt="3" src="https://github.com/user-attachments/assets/2427158a-9d2c-4a99-9271-6174041392db" />

---

## Hash Preservation

The extracted hash was saved to a text file for reuse during cracking.

Command used:

```bash
echo 'HASH_VALUE' > pdflogs.txt
```

I intentionally used **single quotes** because characters such as `$` and `*` may be interpreted by the shell when double quotes are used.

To verify the saved hash, I used:

```bash
cat pdflogs.txt
```

I also learned that the **entire hash string** generated by pdf2john must be saved, not only the hexadecimal portion.

### Screenshot Placeholder

<img width="1155" height="94" alt="5" src="https://github.com/user-attachments/assets/8049db57-d86e-4d3a-a80c-4a8d3b50b455" />

---

## Hash Mode Identification

To identify supported PDF hash modes in Hashcat, I used:

```bash
hashcat --help | grep -i pdf
```

This displayed the available PDF hash modes.

<img width="674" height="194" alt="6" src="https://github.com/user-attachments/assets/a99f787b-6010-4db6-bc17-d30eca92c53c" />

*Insert Screenshot 04 - Hashcat PDF hash modes output*

---

## PDF Version Identification

I initially believed the correct mode was **10400**, but further testing showed that assumption was incorrect.

To identify the PDF encryption details, I used:

```bash
pdfinfo logs_protected.pdf
```

I researched the relationship between the **algorithm version** and **revision** values and the corresponding Hashcat mode.

---

## Hashcat Testing

Because the initially selected mode did not work, I tested the hash against the different PDF modes returned by:

```bash
hashcat --help | grep -i pdf
```

This confirmed that choosing the wrong mode prevents recovery even when the correct password exists in the wordlist.

---

## Password Recovery

The hash was eventually cracked using a different PDF hash mode.

Example command:

```bash
hashcat -m [HASH_MODE] pdf_hash.txt wordlist.txt
```

To display the recovered password, I used:

```bash
hashcat -m [HASH_MODE] pdf_hash.txt --show
```

### Screenshot Placeholder

<img width="1143" height="466" alt="10" src="https://github.com/user-attachments/assets/348f1a6b-b52b-41ba-b505-e979e1b10eec" />

---

## Verification

The recovered password successfully opened `logs_protected.pdf`.

### Screenshot Placeholder

<img width="924" height="964" alt="11" src="https://github.com/user-attachments/assets/3a8c25b8-327c-4166-abf4-915823035272" />

---

## Tools Used

- pdf2john.py
- Hashcat
- pdfinfo
- cat
- echo
- Kali Linux terminal

---

## Lessons Learned

- File type must be identified before selecting a hash-extraction tool.
- `office2john` is not suitable for PDF files.
- `pdf2john.py` is the correct extraction tool for protected PDFs.
- Save the **full extracted hash**, not a partial value.
- Use **single quotes** when saving hashes containing shell-sensitive characters.
- Verify saved hashes before cracking.
- Selecting the correct Hashcat mode is critical.
- `pdfinfo` can assist with identifying PDF encryption details.
- Practical testing may still be required when mode identification is uncertain.

---

## Conclusion

The password-recovery exercise was completed successfully. The protected PDF hash was extracted, preserved correctly, tested against multiple PDF hash modes, cracked with Hashcat, and the recovered password was verified by opening the document. The investigation reinforced the importance of correct tool selection, careful hash handling, and accurate mode identification during password-recovery operations.

---

## Appendix

### Key Commands

```bash
office2john logs_protected.pdf
pdf2john.py logs_protected.pdf
echo 'HASH_VALUE' > pdf_hash.txt
cat pdf_hash.txt
hashcat --help | grep -i pdf
pdfinfo logs_protected.pdf
hashcat -m [HASH_MODE] pdf_hash.txt wordlist.txt
hashcat -m [HASH_MODE] pdf_hash.txt --show
```
